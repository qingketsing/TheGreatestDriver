/upload/chunk 断点续传部分，按块上传独立实现，对数据库与元数据逻辑做小幅扩展（file_hash、storage_path、引用计数等），以支持秒传去重与合并后的元数据写入，避免重复物理文件。

/upload/quick：客户端先计算文件哈希并询问服务端是否已存在相同文件（秒传），若存在直接创建元数据/索引记录并返回成功，否则返回需要上传。

/upload/progress/:uploadId：返回上传会话进度（已接收块数、总块数、状态、已接收字节等）。


本地部署
接口对接
登录验证部分